# v1.8 自定义起始周修改包

本文件包含需要复制到项目中的完整 v1.8 修改代码片段。

## 1. index.html 插入区块

放在「体重记录」section 后，「周平均趋势」section 前。

```html
<!-- ==============================
     v1.8 自定义趋势起始周
     ============================== -->
<section class="card trend-start-card">
    <h2>趋势起始周</h2>

    <p class="settings-action-text">
        可以把正式减脂、备赛或增肌开始的日期设为起点。「较起始周」会按照这个日期所在的周来计算。
    </p>

    <div class="form-group">
        <label for="trendStartDateInput">趋势起始日</label>
        <input id="trendStartDateInput" type="date" />
    </div>

    <div class="trend-start-button-grid">
        <button id="saveTrendStartDateButton" class="primary-button" type="button">
            保存起始日
        </button>

        <button id="clearTrendStartDateButton" class="setting-button secondary-button" type="button">
            清除设置
        </button>
    </div>

    <p class="help-text" id="trendStartDateHelpText">
        未设置时，会自动使用第一条体重记录所在周作为起始周。
    </p>
</section>
```

## 2. index.html 修复负数输入

把：

```html
<input id="manualCalorieOffset" inputmode="numeric" placeholder="例如 -450" type="number" />
```

改成：

```html
<input id="manualCalorieOffset" inputmode="text" placeholder="例如 -450" type="text" />
```

## 3. style.css 追加到底部

```css
/* ==============================
   v1.8 自定义趋势起始周
   ============================== */

.trend-start-card {
    margin-top: 16px;
}

.trend-start-button-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 12px;
    margin: 6px 0 10px;
}

.trend-start-button-grid .primary-button,
.trend-start-button-grid .setting-button {
    width: 100%;
}

#trendStartDateInput {
    font-weight: 700;
}

#trendStartDateHelpText {
    margin-top: 10px;
}
```

## 4. script.js 修改点

### 4-1 DEFAULT_SETTINGS 加入

```javascript
trendStartDate: "",
```

### 4-2 DOM 元素加入

```javascript
const trendStartDateInput = document.getElementById("trendStartDateInput");
const saveTrendStartDateButton = document.getElementById("saveTrendStartDateButton");
const clearTrendStartDateButton = document.getElementById("clearTrendStartDateButton");
const trendStartDateHelpText = document.getElementById("trendStartDateHelpText");
```

### 4-3 在 getFirstRecordedWeekStartDateText 后加入

```javascript
/**
 * v1.8 自定义起始日を保存する
 */
function saveTrendStartDate() {
    const trendStartDate = trendStartDateInput ? trendStartDateInput.value : "";

    if (!trendStartDate) {
        alert("请选择趋势起始日。");
        return;
    }

    const settings = {
        ...loadSettings(),
        trendStartDate,
    };

    saveSettings(settings);
    updateTrendStartDateDisplay();
    updateWeightDisplay();

    alert("趋势起始日已保存。");
}

/**
 * v1.8 自定义起始日を清除する
 */
function clearTrendStartDate() {
    const settings = {
        ...loadSettings(),
        trendStartDate: "",
    };

    saveSettings(settings);
    updateTrendStartDateDisplay();
    updateWeightDisplay();

    alert("趋势起始日已清除。将重新使用第一条体重记录所在周。");
}

/**
 * v1.8 有效起始周を取得する
 *
 * 有自定义起始日：使用自定义日期所在周
 * 没有自定义起始日：使用第一条体重记录所在周
 *
 * @returns {string | null}
 */
function getEffectiveTrendStartWeekDateText() {
    const settings = loadSettings();

    if (settings.trendStartDate) {
        return getWeekStartDateText(settings.trendStartDate);
    }

    return getFirstRecordedWeekStartDateText();
}

/**
 * v1.8 起始日显示を更新する
 */
function updateTrendStartDateDisplay() {
    if (!trendStartDateInput || !trendStartDateHelpText) {
        return;
    }

    const settings = loadSettings();
    const trendStartDate = settings.trendStartDate || "";
    const effectiveWeekStart = getEffectiveTrendStartWeekDateText();

    trendStartDateInput.value = trendStartDate;

    if (trendStartDate) {
        trendStartDateHelpText.textContent = `当前起始周：${formatDisplayDate(effectiveWeekStart)} 开始。`;
        return;
    }

    if (effectiveWeekStart) {
        trendStartDateHelpText.textContent = `未手动设置，当前使用第一条体重记录所在周：${formatDisplayDate(effectiveWeekStart)} 开始。`;
        return;
    }

    trendStartDateHelpText.textContent = "未设置时，会自动使用第一条体重记录所在周作为起始周。";
}
```

### 4-4 updateWeightDisplay 内替换

把：

```javascript
const firstWeekStart = getFirstRecordedWeekStartDateText();
```

改成：

```javascript
const firstWeekStart = getEffectiveTrendStartWeekDateText();
```

### 4-5 saveSelectedDateWeight 内保存后刷新

把：

```javascript
saveWeightRecords(filteredRecords);
updateWeightDisplay();

alert("体重已保存");
```

改成：

```javascript
saveWeightRecords(filteredRecords);
updateTrendStartDateDisplay();
updateWeightDisplay();

alert("体重已保存");
```

### 4-6 备份版本号

把：

```javascript
version: "1.7",
```

改成：

```javascript
version: "1.8",
```

### 4-7 importBackupData 末尾刷新

把：

```javascript
fillTargetSettingsForm();
renderCalendar();
renderSelectedDateRecord();
updateTargetDisplay();
updateWeightDisplay();

alert("数据导入完成。");
```

改成：

```javascript
fillTargetSettingsForm();
renderCalendar();
renderSelectedDateRecord();
updateTargetDisplay();
updateTrendStartDateDisplay();
updateWeightDisplay();

alert("数据导入完成。");
```

### 4-8 事件绑定加入

```javascript
if (saveTrendStartDateButton) {
    saveTrendStartDateButton.addEventListener("click", saveTrendStartDate);
}

if (clearTrendStartDateButton) {
    clearTrendStartDateButton.addEventListener("click", clearTrendStartDate);
}
```

### 4-9 初始化加入

把：

```javascript
renderCalendar();
renderSelectedDateRecord();
updateTargetDisplay();
updateWeightDisplay();
```

改成：

```javascript
renderCalendar();
renderSelectedDateRecord();
updateTargetDisplay();
updateTrendStartDateDisplay();
updateWeightDisplay();
```

## 5. service-worker.js

把缓存版本改成：

```javascript
const CACHE_NAME = "carbon-cycle-memo-v8";
```

## 6. 提交命令

```bash
git add index.html style.css script.js service-worker.js
git commit -m "Add custom trend start week"
git push
```
